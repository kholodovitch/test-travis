language: python
python: "2.7"
sudo: required

branches:
  only:
    - master
    - develop

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y git-annex

script: npm install -g grunt-cli

